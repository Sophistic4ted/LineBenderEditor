import { Tile } from "./Tile.js";

export class Line {
    constructor(public start: {x: number, y: number}, public tiles: Tile[] = []) {}
  
    // TODO: Add methods for moving the line, extending the line etc.
  }