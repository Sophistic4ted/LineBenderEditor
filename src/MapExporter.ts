import { GridEditor } from "./GridEditor.js";
export class MapExporter {
    constructor(private gridEditor: GridEditor) {}
  
    // TODO: Add method for exporting the current view to the specified format.
  }